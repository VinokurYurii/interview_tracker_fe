services:
  app:
    build:
      context: .
      target: development
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://localhost:3000
    stdin_open: true
    tty: true
